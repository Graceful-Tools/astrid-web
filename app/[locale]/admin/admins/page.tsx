// Canonical direct child route. Keep the previous nested route working as a
// compatibility link while the shared admin navigation points here.
export { default, dynamic } from '../analytics/admins/page'
